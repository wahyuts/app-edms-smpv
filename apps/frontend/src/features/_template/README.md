# Feature Template

This directory is the official implementation template for every feature in EDMS.

Every new feature must follow this structure.

Do not implement business logic inside this template.

This directory is used as a blueprint only.